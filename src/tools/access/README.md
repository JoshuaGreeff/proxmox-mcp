# Access Tools

This folder contains access and identity MCP tool registrations.

Current tools:
- `proxmox_user_list`

These tools map to access-control families such as:
- `/access/users`

Transport preference:
- REST first

Validation boundary:
- keep these tools close to Proxmox identity resources and ACL semantics
