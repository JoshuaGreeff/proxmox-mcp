# TODO

- Evaluate GitHub's official remote MCP server implementation as a production reference. Decide which parts are worth adopting for `proxmox-mcp`, especially around auth, scoped capability exposure, read-only modes, toolset narrowing, auditability, and whether an optional hosted deployment mode makes sense without weakening the current `stdio`-first, operator-owned architecture.
